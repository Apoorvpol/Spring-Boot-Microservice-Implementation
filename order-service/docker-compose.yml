services:

  mysql:
    image: mysql:8.3.0
    container_name: mysql-db-order-service

    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3317:3306"

    volumes:
      - ./mysql:/var/lib/mysql
      - ./Docker/Mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
